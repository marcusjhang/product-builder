schema_version: "1.1"
name: playbook-investigation
tags: ["playbook", "tier2", "pbA"]
runs: 1
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 60
  timeout_seconds: 900
  allowed_tools: [Read, Glob, Grep, Skill, Agent, TodoWrite]
