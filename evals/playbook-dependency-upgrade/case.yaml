schema_version: "1.1"
name: playbook-dependency-upgrade
tags: ["playbook", "tier2", "flow", "team"]
runs: 1
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 120
  timeout_seconds: 2400
  allowed_tools: [Read, Glob, Grep, Skill, Agent, TodoWrite]
