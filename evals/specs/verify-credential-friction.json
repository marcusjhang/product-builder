{
  "skills": [
    "product-builder-verify"
  ],
  "query": "verify a slice where the database credential in the environment answers \"password authentication failed\"",
  "expected_behavior": [
    "the failure is named in one line as a friction event",
    "the detected local alternatives (a scratch clone of a local database, the preview environment, a mock) are listed and one question is asked with the cheapest real path recommended",
    "a mock that would ship is called the user's call and the preview environment is preferred over it",
    "the answer is recorded in the ledger"
  ]
}
