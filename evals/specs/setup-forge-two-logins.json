{
  "skills": [
    "product-builder-setup"
  ],
  "query": "setup in a repo where gh auth status lists two logins and git config credential.username names the one that is not active",
  "expected_behavior": [
    "the Forge section names which login this remote uses and how it was determined",
    "the profile says the login's token is used through the tool's per-user token command and that a token is never written to a file in the repo",
    "the user is not asked to switch the active account"
  ]
}
