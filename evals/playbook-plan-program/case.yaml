schema_version: "1.1"
name: playbook-plan-program
tags: ["playbook", "tier2", "flow", "pbB", "retest1"]
runs: 1
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 150
  timeout_seconds: 2400
  allowed_tools: [Read, Glob, Grep, Skill, Agent, TodoWrite]
