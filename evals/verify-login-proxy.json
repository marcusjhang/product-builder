{
  "skills": [
    "product-builder-verify"
  ],
  "query": "verify a UI slice in a repo whose login page redirects to another host and whose drive.md has readiness and login as UNKNOWN",
  "expected_behavior": [
    "the first run validates drive.md and writes the outcome back",
    "the redirect is not followed; a session is minted per the repo's documented recipe and set as a cookie in the driver, and the recipe is recorded in drive.md",
    "the reply says which fields of drive.md were filled"
  ]
}
