---
type: 'llm'
focus: 'trace'
---

PASS if the run's replies say the archive route already exists on the default branch behind the archive flag (the probe verdict partial or a similar statement) and the plan is only the page control. FAIL if the plan re-implements the route or never mentions that it exists.
