schema_version: "1.1"
name: playbook-ship
tags: ["playbook", "tier2", "flow"]
runs: 1
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 100
  timeout_seconds: 1800
  allowed_tools: [Read, Glob, Grep, Skill, Agent, TodoWrite]
