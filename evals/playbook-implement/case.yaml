schema_version: "1.1"
name: playbook-implement
tags: ["playbook", "tier2", "flow", "team", "pbB", "retest2"]
runs: 1
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 200
  timeout_seconds: 3600
  allowed_tools: [Read, Glob, Grep, Skill, Agent, TodoWrite]
