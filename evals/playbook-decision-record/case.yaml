schema_version: "1.1"
name: playbook-decision-record
tags: ["playbook", "tier2"]
runs: 1
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 60
  timeout_seconds: 1200
  allowed_tools: [Read, Glob, Grep, Skill, Agent, TodoWrite]
