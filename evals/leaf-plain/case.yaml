schema_version: "1.1"
name: leaf-plain
tags: ["leaf", "tier1"]
runs: 1
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 5
  timeout_seconds: 300
  allowed_tools: [Read, Glob, Grep, Skill, Agent, TodoWrite]
