schema_version: "1.1"
name: route-explicit-prefix
tags: ["routing", "tier1"]
runs: 1
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 30
  timeout_seconds: 600
  allowed_tools: [Read, Glob, Grep, Skill, Agent, TodoWrite]
