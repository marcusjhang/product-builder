{
  "description": "product-builder hooks. first-run: at session start, one line when the repo has no .product-builder/profile.md (silent otherwise, or with PRODUCT_BUILDER_NUDGE=0). question-gate: runs the profile's judge over every question before the question tool opens; silent when the profile names no judge or no key is set.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/skills/product-builder/scripts/first-run\"",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "AskUserQuestion",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/skills/product-builder/scripts/question-gate\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
